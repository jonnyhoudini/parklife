# MongoDB connection string (Atlas or self-hosted). Example:
# ATLAS_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net
ATLAS_URI=mongodb+srv://your_user:your_password@cluster0.xxxxx.mongodb.net/?appName=Cluster0

# Optional: database name to use
DB_NAME=parklife

# Server port
PORT=3000

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://cllrmcphilemy.vercel.app

# Vercel Blob storage token for image uploads (required for production)
# Get this from Vercel Dashboard -> Storage -> Blob Store
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxx

# Node environment (development allows any origin for CORS)
NODE_ENV=development
